//! This modules contains two types we copy from the IC repository: ICPTs and the Account identifier.
//! We apply simply adjustments to the copied file to make them compile without warnings.

pub mod account_identifier;
pub mod icpts;
